import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Application_3-1_practica',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
